const state = {
  localUrl: "http://localhost:5000/",
  // localUrl: "https://excel-server-mark0002.mekmar.com/",
};
const actions = {};

const mutations = {};

const getters = {
  getLocalUrl(state) {
    return state.localUrl;
  },
};
export default {
  state,
  actions,
  mutations,
  getters,
};
